// CalendarIcon.js

import React from 'react';

const SearchIcon = () => {
    return (
        <span className="material-symbols-outlined icon-hover">
            calendar_month
        </span>
    );
};

export default SearchIcon;