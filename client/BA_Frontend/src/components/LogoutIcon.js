import React from 'react';

const LogoutIcon = () => {
    return (
        <span className="material-symbols-outlined icon-hover">
            logout
        </span>
    );
};

export default LogoutIcon;